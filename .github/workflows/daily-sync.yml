name: Candidate sync (every 6h)

# Runs every 6 hours: 00:17, 06:17, 12:17, 18:17 UTC
# = 5:47 AM, 11:47 AM, 5:47 PM, 11:47 PM IST
on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch: {}

jobs:
  sync:
    runs-on: ubuntu-latest
    # Generous: SINCE_DATE never rolls forward, so a first catch-up run after
    # any gap re-scans the mailbox's full history since Jan 1. The header-only
    # dedup check in gmail_client.py keeps already-recorded messages cheap,
    # but a large batch of genuinely new ones (each a full email + attachment
    # download) can still take a while. Backstop only - not the expected time.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run sync
        env:
          PYTHONUNBUFFERED: "1"
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GCP_SERVICE_ACCOUNT_JSON: ${{ secrets.GCP_SERVICE_ACCOUNT_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
        run: python src/main.py
